spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: rlaalstn123
    url: jdbc:mysql://localhost:3306/c4_assignment?socketTimeout=2000
    hikari:
      connection-timeout: 3000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

cloud:
  aws:
    credentials:
      access-key:
      secret-key:
    s3:
      bucket: minsubucket23
    region:
      static: ap-northeast-2
      auto: false
    stack:
      auto: false

image:
  prefix: https://minsubucket23.s3.ap-northeast-2.amazonaws.com


